const styles = (theme) => ({
    cent: {
        width: "100%",
        height: "100%",
        ...theme.alignment.horizontalMarginCenter,
        ...theme.alignment.verticalMarginCenter,
    }
})

export default styles;